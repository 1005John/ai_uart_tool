#!/usr/bin/env python3
"""提示词模板 - AI Native UART Tool"""


SYSTEM_PROMPT = """你是 AI Native UART Tool 的内置 AI 助手，专为物联网模组 AT 指令自动化测试而设计。

## 你的能力

### 你可以做的事情
1. **串口控制** — 扫描串口、打开/关闭、配置波特率
2. **测试执行** — 从已有的测试用例库加载用例并执行
3. **测试分析** — 分析 FAIL 结果，给出根因判断
4. **缺陷管理** — 生成缺陷描述、提交到灵畿平台
5. **知识查询** — 查询历史测试记录、模组能力参数

### 当前可用的测试模块
{modules_list}

### 测试用例分级
- **P0**: 冒烟测试（核心功能，必须通过）
- **P1**: 详细功能验证
- **P2**: 边界/异常场景

## 交互规范

1. 当用户提出测试需求时，**先展示测试计划**，包含用例数、级别分布、预计耗时
2. 执行结果**实时反馈**，统计 PASS/FAIL
3. 每条 FAIL 自动给出**初步分析**（是模组 bug 还是环境问题）
4. 用户可随时要求**调整参数后重新执行**
5. 用户确认缺陷后，**自动生成符合格式的缺陷描述**
6. 用中文回答，简洁专业"""


TEST_PLAN_PROMPT = """基于以下测试用例生成测试计划：

模块: {module}
适用模组: {model}
用例级别: {case_level}
服务器: 8.137.154.246

用例列表:
{cases_list}

请生成简洁的测试计划总结，包含：
1. 测试范围概述
2. 用例数量统计（按级别）
3. 执行前需要确认的配置项
4. 预计执行时间"""


FAIL_ANALYSIS_PROMPT = """分析以下 AT 指令测试的 FAIL 结果：

测试用例: {case_name}
AT 指令: {at_command}
预期结果: {expected}
实际返回: {actual}
模组型号: {model}
测试服务器: 8.137.154.246

请分析：
1. 这是模组 bug 还是测试环境/配置问题？
2. 如果是 bug，可能的根因是什么？
3. 严重程度（1-4，4最严重）
4. 建议

分析要简洁专业，200字以内。"""


DEFECT_DESCRIPTION_PROMPT = """根据测试 FAIL 结果生成标准缺陷报告，严格按以下格式：

## 缺陷报告

### 测试环境
- 模组: {model}
- 模块: {module}
- 用例: {case_name}
- AT指令: {at_command}

### 复现步骤
1. {at_command}

### 预期结果
{expected}

### 实际结果
{actual}

### 根因分析
{ai_analysis}

要求：语言简洁专业，根因分析指出可能的代码/配置/环境原因。"""


INTENT_CLASSIFICATION_PROMPT = """判断用户意图，只返回一个词：

- serial: 串口操作（打开、关闭、扫描、配置波特率等）
- test: 测试相关（执行测试、查看用例、结果分析、测、跑、验证）
- defect: 缺陷管理（创建缺陷、提交、查看）
- knowledge: 知识查询（AT指令含义、模组参数、历史记录）
- greeting: 问候、闲聊
- help: 帮助
- unknown: 无法判断

用户输入: {user_input}

意图:"""
